<% content_for :title, "Editing movie" %>

<h1>Editando o filme</h1>

<%= render "form", movie: @movie %>

<br>

<div>
  <%= link_to t("movies.index.show"), @movie %> |
  <%= link_to t("movies.show.back"), movies_path %>
</div>
