<p style="color: green"><%= notice %></p>

<% content_for :title, I18n.t("movies.index.title") %>

<h1><%= I18n.t("movies.index.title") %></h1>

<div id="movies">
  <% @movies.each do |movie| %>
    <%= render movie %>
    <p>
      <%= link_to I18n.t("movies.index.show"), movie %>
    </p>
  <% end %>
</div>

<%= link_to I18n.t("movies.index.new_movie"), new_movie_path %>
